services:
  pythonweb:
    image: python:3.11-slim
    container_name: python_web
    working_dir: /usr/src/app
    volumes:
      - ./index.html:/usr/src/app/index.html:ro
    command: python -m http.server 8080
    ports:
      - "8080:8080"
    restart: unless-stopped
